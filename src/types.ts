export interface PropertySpec {
  label: string;
  value: string;
  icon?: string;
}

export interface GalleryImage {
  src: string;
  alt: string;
}

export interface HeroSlide {
  image: string;
  alt: string;
}